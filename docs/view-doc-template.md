# <ViewName>

## 简介
简要描述视图用途、适用场景以及核心特性。

## 参数说明
| 参数键 | 说明 | 默认值 | 备注 |
| --- | --- | --- | --- |
| `location_x` | 视图左上角的 X 坐标 | 无 | 基础参数，必填 |
| `location_y` | 视图左上角的 Y 坐标 | 无 | 基础参数，必填 |
| `width` | 视图宽度 | 无 | |
| `height` | 视图高度 | 无 | |
| *(在此列出额外参数)* |  |  |  |

> 建议：从 `_BaseView.DEFAULT_PARAMS` 继承的四个基础参数放在表格开头，然后添加该视图专属参数。

## 渲染逻辑
- 说明绘制步骤、核心算法或注意事项。
- 如果依赖外部服务、字体或额外资源，请写明处理方式与失败策略。
- 可在需要时补充流程图、示意图链接等材料。

## 示例配置
```json
{
  "type": "<ViewName>",
  "location_x": 0,
  "location_y": 0,
  "width": 100,
  "height": 100,
  "custom_param": "示例值"
}
```

## 相关参考
- `src/canvas_runtime/views/<view_name>.py`
- 若视图依赖其它模块，请补充链接，方便查阅源码。
